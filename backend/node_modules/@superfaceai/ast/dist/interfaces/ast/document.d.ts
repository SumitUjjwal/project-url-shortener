export declare const EXTENSIONS: {
    profile: {
        source: string;
        build: string;
    };
    map: {
        source: string;
        build: string;
    };
};
export declare const enum DocumentType {
    UNKNOWN = "unknown",
    MAP = "map",
    PROFILE = "profile",
    MAP_AST = "map.ast",
    PROFILE_AST = "profile.ast"
}
/**
 * Detects whether the file on path is Superface Map or Superface Profile based on the extension.
 */
export declare function inferDocumentType(path: string): DocumentType;
/**
 * Checks whether the file is a Profile source file
 **/
export declare function isProfileFile(file: string): boolean;
/**
 * Checks whether the file is a compiled Profile file
 **/
export declare function isProfileASTFile(file: string): boolean;
/**
 * Checks whether the file is a Map source file
 **/
export declare function isMapFile(file: string): boolean;
/**
 * Checks whether the file is a compiled Map file
 **/
export declare function isMapASTFile(file: string): boolean;
/**
 * Checks whether the file is neither map nor profile file
 **/
export declare function isUnknownFile(file: string): boolean;
