import { AnySchema } from 'ajv';
import { Assert, Guard } from './interfaces';
export declare function preparePrepareIs(schema: AnySchema): <T>(id: string) => Guard<T>;
export declare function prepareAssert<T>(schema: AnySchema, key: string): Assert<T>;
