export declare class SuperCache<T> {
    private cache;
    getCached(cacheKey: string, initializer: () => T): T;
    getCached(cacheKey: string, initializer: () => Promise<T>): Promise<T>;
    invalidate(cacheKey: string): void;
}
