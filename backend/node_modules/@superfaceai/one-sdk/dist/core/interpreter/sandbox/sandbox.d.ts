import type { IConfig, ILogger } from '../../../interfaces';
import type { NonPrimitive } from '../../../lib';
export declare function evalScript(config: IConfig, js: string, logger?: ILogger, variableDefinitions?: NonPrimitive): unknown;
