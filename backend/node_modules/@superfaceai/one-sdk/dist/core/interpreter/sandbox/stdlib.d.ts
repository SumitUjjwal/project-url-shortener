import type { ILogger, LogFunction } from '../../../interfaces';
declare const STDLIB: (debugLog?: LogFunction | undefined) => import("../../../lib").RecursiveReadonly<{
    unstable: {
        time: {
            isoDateToUnixTimestamp(iso: string): number;
            unixTimestampToIsoDate(unix: number): string;
        };
        debug: {
            log(formatter: string, ...args: unknown[]): void;
        };
    };
}>;
export declare function getStdlib(logger?: ILogger): ReturnType<typeof STDLIB>;
export {};
