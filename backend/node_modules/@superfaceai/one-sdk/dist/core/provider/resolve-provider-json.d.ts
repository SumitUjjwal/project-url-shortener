import type { NormalizedSuperJsonDocument, ProviderJson } from '@superfaceai/ast';
import type { IConfig, IFileSystem, ILogger } from '../../interfaces';
export declare function resolveProviderJson({ providerName, superJson, fileSystem, logger, config, }: {
    providerName: string;
    logger?: ILogger;
    superJson: NormalizedSuperJsonDocument | undefined;
    fileSystem: IFileSystem;
    config: IConfig;
}): Promise<ProviderJson | undefined>;
