import type { SecurityValues } from '@superfaceai/ast';
import type { IProvider } from '../../interfaces';
export declare class ProviderConfiguration {
    readonly name: string;
    /** @deprecated only for use in testing library */
    readonly security: SecurityValues[];
    readonly parameters?: Record<string, string>;
    constructor(name: string, security: SecurityValues[], parameters?: Record<string, string>);
    get cacheKey(): string;
}
export declare class Provider implements IProvider {
    readonly configuration: ProviderConfiguration;
    constructor(configuration: ProviderConfiguration);
    /** @deprecated */
    configure(configuration?: {
        security?: SecurityValues[];
    }): Promise<Provider>;
}
