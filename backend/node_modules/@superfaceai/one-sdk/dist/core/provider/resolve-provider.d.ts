import type { NormalizedSuperJsonDocument, SecurityValues } from '@superfaceai/ast';
import { Provider } from './provider';
/**
 * Resolves ProviderConfiguration from parameters.
 * Fallbacks to SuperJson information if provider not specified
 */
export declare function resolveProvider({ provider, security, parameters, superJson, profileId, }: {
    security?: SecurityValues[];
    parameters?: Record<string, string>;
    superJson?: NormalizedSuperJsonDocument;
    provider?: string | Provider;
    profileId?: string;
}): Provider;
