"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveMapAst = void 0;
var ast_1 = require("@superfaceai/ast");
var errors_1 = require("../errors");
var DEBUG_NAMESPACE = 'map-file-resolution';
function resolveMapAst(_a) {
    var profileId = _a.profileId, providerName = _a.providerName, variant = _a.variant, superJson = _a.superJson, fileSystem = _a.fileSystem, logger = _a.logger, config = _a.config;
    return __awaiter(this, void 0, void 0, function () {
        var profileSettings, profileProviderSettings, log, path, contents, ast, astProfileId;
        return __generator(this, function (_b) {
            switch (_b.label) {
                case 0:
                    if (superJson === undefined) {
                        return [2 /*return*/, undefined];
                    }
                    profileSettings = superJson.profiles[profileId];
                    if (profileSettings === undefined) {
                        throw errors_1.profileNotFoundError(profileId);
                    }
                    profileProviderSettings = profileSettings.providers[providerName];
                    if (profileProviderSettings === undefined) {
                        throw errors_1.profileProviderNotFoundError(profileId, providerName);
                    }
                    log = logger === null || logger === void 0 ? void 0 : logger.log(DEBUG_NAMESPACE);
                    if (!('file' in profileProviderSettings)) return [3 /*break*/, 5];
                    path = void 0;
                    if (!profileProviderSettings.file.endsWith(ast_1.EXTENSIONS.map.source)) return [3 /*break*/, 2];
                    path = fileSystem.path.resolve(fileSystem.path.dirname(config.superfacePath), profileProviderSettings.file.replace(ast_1.EXTENSIONS.map.source, ast_1.EXTENSIONS.map.build));
                    return [4 /*yield*/, fileSystem.exists(path)];
                case 1:
                    // check if ast exists to print usefull error (needs to be compiled)
                    if (!(_b.sent())) {
                        throw errors_1.sourceFileExtensionFoundError(ast_1.EXTENSIONS.map.source);
                    }
                    return [3 /*break*/, 3];
                case 2:
                    if (profileProviderSettings.file.endsWith(ast_1.EXTENSIONS.map.build)) {
                        path = fileSystem.path.resolve(fileSystem.path.dirname(config.superfacePath), profileProviderSettings.file);
                    }
                    else {
                        throw errors_1.unsupportedFileExtensionError(profileProviderSettings.file, ast_1.EXTENSIONS.map.source);
                    }
                    _b.label = 3;
                case 3:
                    log === null || log === void 0 ? void 0 : log("Reading compiled map from path: \"" + path + "\"");
                    return [4 /*yield*/, fileSystem.readFile(fileSystem.path.resolve(fileSystem.path.dirname(config.superfacePath), path))];
                case 4:
                    contents = _b.sent();
                    if (contents.isErr()) {
                        throw errors_1.referencedFileNotFoundError(path, []);
                    }
                    ast = ast_1.assertMapDocumentNode(JSON.parse(contents.value));
                    // check if variant match
                    if (variant !== ast.header.variant) {
                        throw errors_1.variantMismatchError(ast.header.variant, variant);
                    }
                    // check if provider name match
                    if (providerName !== ast.header.provider) {
                        throw errors_1.providersDoNotMatchError(ast.header.provider, providerName, 'map');
                    }
                    astProfileId = ast.header.profile.scope !== undefined
                        ? ast.header.profile.scope + "/" + ast.header.profile.name
                        : ast.header.profile.name;
                    if (astProfileId !== profileId) {
                        throw errors_1.profileIdsDoNotMatchError(astProfileId, profileId);
                    }
                    return [2 /*return*/, ast];
                case 5: return [2 /*return*/, undefined];
            }
        });
    });
}
exports.resolveMapAst = resolveMapAst;
//# sourceMappingURL=resolve-map-ast.js.map