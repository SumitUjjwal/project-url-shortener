import type { MapDocumentNode, NormalizedSuperJsonDocument } from '@superfaceai/ast';
import type { IConfig, IFileSystem, ILogger } from '../../interfaces';
export declare function resolveMapAst({ profileId, providerName, variant, superJson, fileSystem, logger, config, }: {
    profileId: string;
    providerName: string;
    variant?: string;
    logger?: ILogger;
    superJson: NormalizedSuperJsonDocument | undefined;
    fileSystem: IFileSystem;
    config: IConfig;
}): Promise<MapDocumentNode | undefined>;
