/// <reference types="node" />
import type { ITimeout, ITimers } from '../../core';
export declare class NodeTimeout implements ITimeout {
    value: NodeJS.Timeout;
    constructor(value: NodeJS.Timeout);
}
export declare class NodeTimers implements ITimers {
    setTimeout(callback: (...args: unknown[]) => unknown, timeout: number): ITimeout;
    clearTimeout(timeout: NodeTimeout): void;
    sleep(ms: number): Promise<void>;
    now(): number;
}
